---
tags: [code, files]
summary: flat map of all source files in rpg-wibe project (path — role)
updated: 2026-05-23
---

- one bullet per file in the godot project; format: `res://path/to/file.ext — what this file is`
- order by path; group by folder visually but no headings
- add the bullet in the same change that adds the file; remove on delete; patch on rename
- entries:
    - none yet
